{
  "name": "neon-chaos-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for NEON CHAOS NFT marketplace",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy-simple.js --network sepolia",
    "verify": "hardhat verify --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1"
  }
}
